_site
.jekyll-cache
node_modules
.claude
build
.docusaurus
scripts/output/
.cache-loader
.env

# Yarn Berry (Corepack-managed, node-modules linker + global cache)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# npm is blocked (see preinstall guard); never commit an npm lockfile
package-lock.json

scripts/.yarn/

# Videos are no longer committed to the repo (old tutorials under videos/ remain
# tracked for history; gitignore doesn't affect tracked files). All new video
# work is local-only — for Mautic tutorials, support calls, or personal reference.
.DS_Store
videos/
